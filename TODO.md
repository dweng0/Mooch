### out

- need a way to allow oauth sign in on the discord app.
- need a way for users to provide their own key, which means we need to support diffrerent LLMs, for now we can say we only support gemini, claude, openai. it should still hit the backend, then the backend will send it off.
- stripe is integrated but needs to be tested
- oauths are integrated but need to be testing
- once oauth testing is done we need to test you can login using your oauth on the discord app.
- lets put "coming soon" for the  mock interview button

- we need to add a " free " options for users who want to bring their own api key.
- users who are free we need to consider advertising.
- pages like "subscription plan" and "login" do not look good currently and need updating so they look just as amazing as the landing page.
- check wording, we need to rename "copilot" and "interview copilot" to Mooch, we also need to rename Mochi and pippins mr pippins, to Mooch.
- need a better 404 page.
- the account page also needs updating.
- add some testimonials. maybe get people to use it (from clearmatics).
- alternatively offer a slightly better than free option that can be used if you provide feedback.
